// Q88
// this function rounding a nearst integer with decimal number
function nearstinteger(num : number): number {
return Math.round(num);// round the number
}
console.log(nearstinteger(3.4));// output is 3